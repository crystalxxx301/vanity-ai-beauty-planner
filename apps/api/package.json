{
  "name": "@vanity/api",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "node --env-file-if-exists=.env src/server.ts",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@vanity/contracts": "*",
    "fastify": "^5.6.0"
  },
  "devDependencies": {
    "@types/node": "^24.5.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0"
  }
}
